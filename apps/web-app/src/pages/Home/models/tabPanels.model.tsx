import { type TTabDataRecord } from '@/models/UI'
import { Trans } from 'react-i18next'

export const CHomeTabPanelNames: TTabDataRecord = {
  userFeed: {
    name: 'user-feed',
    traduction: <Trans>recipes</Trans>,
    showTabHeader: true
  }
}
